import { Reciept } from "@/app/context/Reciept";
import React, { useContext, useEffect, useRef } from "react";
const RecieptItems = () => {
  const { recieptNumber } = useContext(Reciept);

  // const allId = espresso.map((item: any) => item?.id);

  // const selectedId = recieptNumber.map((item: any) => item?.id);

  // let filtered = espresso.filter((item: any) => selectedId.includes(item.id));



  return (
    <div>
      
    </div>
  );
};

export default RecieptItems;
